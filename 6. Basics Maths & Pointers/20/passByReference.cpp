#include <iostream>
using namespace std;

// void solve(int& num ) {
//         num  = 50;
// } 

// void solve(int* val) {
//         *val = *val + 1;
// }

// void solve(int*& p) { //pointer is now passed by reference due to ' & ' sign if we use ' &* ' instead of ' *& ' it will show compile error
//         p = p + 1;
// }

int main() {

        // int a = 5;
        // solve(a) ;

        // cout << a << endl;

        // int a = 12;
        // int *p = &a;
        // solve(p);
        // solve(&a); // passing address of a
        // cout << a << endl;

        // int a = 5;
        // int* p = &a;

        // cout << "before " << p << endl; 
        // solve(p);
        // cout << "After " << p << endl;



  return 0;
}