#include<iostream>
using namespace std;
int main(){
    
return 0;
}
class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        // there will be at least one way by which alice will win
        // that means , we should return true
        // because problem is asking is there that alice can win
        return true;
    }
};